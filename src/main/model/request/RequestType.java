package main.model.request;

public enum RequestType {
    COORDINATOR_ALLOCATE_PROJECT,
    COORDINATOR_APPROVE_REQUEST,
    COORDINATOR_CHANGE_SUPERVISOR,
    COORDINATOR_DEREGISTER_STUDENT,
    COORDINATOR_REJECT_REQUEST,
    STUDENT_REGISTRATION,
    STUDENT_DEREGISTRATION,
    STUDENT_CHANGE_TITLE,
}
